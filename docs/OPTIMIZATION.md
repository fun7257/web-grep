# Optimization notes

Proposal-only. These files do **not** change search `andTerms`, SSE event names, or Go handler behavior.

| Doc | Scope |
| --- | --- |
| [OPTIMIZATION-PROPOSAL-WEB.md](./OPTIMIZATION-PROPOSAL-WEB.md) | Front-end / how the SPA talks to `/api` (from [PR #18](https://github.com/fun7257/web-grep/pull/18)) |
| Backend | No proposal on `main` yet. When one lands, link it here. |

Step 1 of the suggested order is this contract PR: align `docs/API.md` and `packages/shared` with the existing wire. Later steps stay in separate PRs.
